<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SelectRoutineActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/selectRoutine_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/select_routine_toolbar"
        app:title="@string/select_title"
        app:titleMarginStart="80dp"
        app:titleTextColor="#FFFFFF"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:background="#EEE200"
        app:navigationIcon="@drawable/arrow_back_fill0_wght400_grad0_opsz48"

        />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/selectRoutine_toolbar"
        android:orientation="horizontal"
        >
        <com.google.android.material.floatingactionbutton.FloatingActionButton

            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/select_add_routineBtn"
            android:layout_margin="15dp"
            android:src="@drawable/add_thick"


            />

        <TextView
            android:layout_width="268dp"
            android:layout_height="45dp"
            android:layout_marginTop="20dp"
            android:text="@string/select_createRoutine"
            android:textSize="25sp" />

    </LinearLayout>





</androidx.constraintlayout.widget.ConstraintLayout>